#!/usr/bin/env ruby

require File.join(File.dirname(__FILE__),'..','lib','run_recipes')

server = ARGV[0]
if server.nil? or server == ""
  puts "Usage: run_recipes <server-name>"
  puts "  rr <server-name> # short hand"
  exit 1
end
RunRecipes.run(server)
